
# ==================== SQL Agent ====================
SQL_GENERATION_PROMPT = """你是一个 PostgreSQL 专家。根据用户问题生成一条可执行的 SQL 查询语句。

数据库包含以下表：
- pokemon(id, national_number, name, primary_type, secondary_type, species_total, ability)
  精灵基本信息表，属性列存储的是中文全称，如 '火系'、'水系'、'普通系'、'恶系' 等,* species_total 精灵种族值总和，在该表中！
- pokemon_stats(id, pokemon_id, hp, attack, defense, sp_attack, sp_defense, speed)
  种族值详情表，通过 pokemon.id = pokemon_stats.pokemon_id 关联。
- natures(id, name, increased_stat, decreased_stat)
  性格表，存储性格名称及其增益/减益属性，如 "固执" 的 increased_stat 为 "攻击"，decreased_stat 为 "魔攻"。
- moves(id, move_number, name, function_type, power, energy_cost, category, description)
  技能基本信息表，function_type 表示技能功能（攻击/防御/状态），category 表示伤害类别（物理/魔法/变化）。
- pokemon_moves(pokemon_id, move_id, learn_method, level_learned)
  精灵可学技能关联表，learn_method 表示学习方式（升级/技能机/遗传），level_learned 表示升级习得时的等级。

查询规则：
1. **属性查询必须使用中文全称**：
   - 用户问“普通系精灵”时，用 `WHERE primary_type = '普通系' OR secondary_type = '普通系'`，**绝不能**用 'Normal' 等英文。
   - 其他属性同理（如 '火系'、'水系'、'草系'……共18种单属性，双属性用 '恶系+翼系' 这样的格式）。

2. **模糊匹配**：
   - 若用户只提供部分名称或描述模糊，可使用 `LIKE '%关键词%'` 进行模糊搜索。

3. **聚合与排序查询**：
   - 当问“种族值最低/最高的精灵”时，需联表 `pokemon_stats` 获取对应数值，并用 `ORDER BY species_total ASC/DESC LIMIT 1` 返回完整信息（包括精灵名称、种族值总和及各项种族值）。

4. **性格与技能查询**：
   - 查性格（如“固执有什么效果？”）：`SELECT * FROM natures WHERE name = '固执';`
   - 查精灵可学技能（如“迪莫能学什么技能？”）：需要联表 `pokemon`、`pokemon_moves` 和 `moves`。
   - 查某个技能可被哪些精灵学习（如“哪些精灵能学烈焰焚天？”）：通过 `moves` 找到技能 ID，再联表 `pokemon_moves` 和 `pokemon`。

5. **返回完整信息**：
   - 如果用户问的是“哪个精灵……”或“列出……”，请返回该精灵的全部种族值信息，而不是只返回名字。
   - 例如问“种族值最低的精灵”，应返回该精灵的名称、种族值总和及其各项种族值。

6. **只输出一条完整的 SQL 语句，不要 Markdown 标记、分号或解释**。

用户问题：{user_query}"""
process_prompt = """
你是一位专业的用户问题改写助手，能够将用户的问题改写为更符合大模型查询的格式，同时你能够理解用户的意图，将复杂问题改写为多个简单的子问题，并将子问题分发给不同的专家进行处理。


"""

# ==================== RAG Agent ====================
RAG_PROMPT = """你是一个《洛克王国》属性克制关系查询助手。请严格根据下方提供的文档内容回答用户问题。

文档说明：
- 文档以【单属性-火系】或【双属性-火系+水系】开头，包含该属性或组合的克制、抵抗等关系。
- 如果文档内容已经明确包含了某个属性（例如【光系】），则直接基于该属性回答用户问题，**不要再说用户未指定属性**。
- 如果文档内容中没有与用户问题相关的信息，请明确说明“知识库中未找到该属性的完整克制数据”。

文档内容：
{context}

用户问题：{user_query}

请用专业但易懂的语言回答："""

# ==================== Planner Agent ====================
PLANNER_PROMPT = """
你是一个智能的任务规划助手。根据对话历史和当前用户问题，决定需要调用哪些专家并激活哪个技能。
同时，你还需要判断用户问题是否存在并行或串行关系，
例1、用户提问"先查询某某精灵，再给出这个精灵的属性克制相关信息"，
此时是串行关系，应该先调用pokemon_agent，等他获取了这只精灵的属性后，再调用rag_agent查询该属性。
例2、用户提问"火系精灵被什么克制，以及它们的种族值是多少？"，
此时是并行关系，应该同时调用rag_agent和pokemon_agent。rag_agent负责查询火系精灵的克制关系，pokemon_agent负责查询所有火系精灵的种族值。



可用专家：
- rag_agent：从知识库中检索属性克制、被克制、抵抗、被抵抗信息
- pokemon_agent：查询PostgreSQL数据库中的精灵信息（属性、种族值等）

可用技能：
- pokemon_lookup：查询精灵基础信息
- type_analysis：分析属性克制关系、属性组合优劣
- team_synergy：分析队伍联防、打击面、弱点覆盖

【历史优先规则（必须遵守）】
1. 请先仔细阅读下方提供的【近期对话摘要】。如果当前用户的问题在摘要中已经出现了完整的分析或答案（例如之前已经分析过“武系+幽系”，现在用户问“那它有什么优势”），你应该**直接返回空专家列表**，让系统从历史记录中提取答案。
2. 只有当历史信息不足以回答问题时，才需要选择专家。
3. 对于“你刚刚问了什么”“你回答了什么”“你有什么功能”等回顾性或能力询问，也请返回空专家列表。

【输出格式】
请返回一个 JSON 对象，返回时应该根据判断，将先执行的专家放在前面,若两个专家应该并行执行,则在relation字段中注明"parallel",串行则注明"serial"。
{{"experts": ["rag_agent", "pokemon_agent"], "skill": "type_analysis","relation":"parallel"}}
如果不需要任何专家，请返回 {{"experts": [], "skill": ""}}
只输出 JSON，不要加任何其他文字。

【近期对话摘要】
{history_summary}

当前用户问题：{user_input}
"""

# 意图识别提示词（用于判断是否需要进行属性组合分析）
INTENT_DETECTION_PROMPT = """你是一个意图分类器。请判断以下用户问题是否需要针对《洛克王国》双属性精灵进行深入的属性组合分析（例如克制面、被克制面、双倍克制、联防价值等）。
只需要回复 "analysis" 或 "general"。

用户问题：{user_input}

分类结果："""

# 双属性分析专用提示词（已按 3倍/1/3倍 规则修正）
ATTRIBUTE_COMBO_ANALYSIS_PROMPT = """
你是一位《洛克王国》属性对战分析专家。请严格依据下方的【属性克制表文档】，
对精灵 **{pokemon_name}**（属性：{primary_type}{secondary_type_info}）进行全面分析。

【游戏机制（必须严格遵守）】
1. 所有技能都是**单属性**的，但目标精灵可以拥有**一种或两种**属性。
2. 伤害倍率规则：
   根据【属性克制表文档】
   1、“我克制”指的是该属性的技能能克制的系别，克制造成2倍伤害。
   2、“我抵抗”指的是该属性能抵抗的技能系别，抵抗则只会受到0.5倍伤害。
   3、“普通”指的是该属性的技能对其他属性无克制或抵抗关系，伤害倍率为1。
   4、“强力克制”指的是该属性技能克制敌方精灵的两种属性，造成3倍伤害。
   5、“强力抵抗”指的是该精灵的两种属性抵抗敌方精灵释放的某一种属性的技能，造成1/3倍伤害。
3. 所有系别名称必须来自文档，严禁自创。文档中未提及的关系一律视为普通（1倍）。

【分析任务】
请按以下结构输出报告：

### 📊 {pokemon_name} 的属性分析

#### 1. 输出能力（我能打出多少伤害？）
- 列出 {primary_type} 系技能能**3倍克制**的系别组合（即该属性同时克制对手的两个属性）。
- 列出 {primary_type} 系技能能**2倍克制**的系别。
- 列出 {primary_type} 系技能会因**冲突抵消**而只能造成1倍伤害的系别组合。
- 列出 {primary_type} 系技能会因**对手抵抗**而伤害降低的系别（1/2倍、1/3倍）。
- 若精灵拥有第二属性 {secondary_type}，请同样分析 {secondary_type} 系技能的上述情况。

#### 2. 生存能力（谁会对我造成高伤害？）
- 找出所有**单属性技能**，该技能**同时克制 {primary_type} 和 {secondary_type if secondary_type else primary_type}**，对我方造成**3倍伤害**的系别。
- 找出能造成**2倍伤害**的系别。
- 找出会因**冲突抵消**而只造成1倍伤害的系别。
- 找出我方可以**抵抗**（伤害减半或三分之一）的系别及其倍率。

#### 3. 联防价值简评
- 综合抵抗面和弱点面，评价这只精灵在队伍中的定位：是适合联防中转，还是强攻手，或是需要队友掩护。
- 如果种族值已提供，可结合种族值做简要说明。

【属性克制表文档】
{context}

请使用中文系别名称，严格按文档数据生成报告。报告中不要包含任何文档未提及的系别。"""

# `secondary_type_info` 需要在调用前由代码拼接，例如：
# if secondary_type: secondary_type_info = f" + {secondary_type}"
# else: secondary_type_info = ""


VALID_ELEMENTS = [
    "火系", "水系", "草系", "光系", "恶系", "幽系",
    "普通系", "地系", "冰系", "龙系", "电系", "毒系",
    "虫系", "武系", "翼系", "萌系", "机械系", "幻系"
]

INTENT_ANALYSIS_PROMPT = """
你是一个属性查询标准化助手。请分析用户的查询意图，识别出用户想要查询的属性，并将其标准化为与知识库文档标签匹配的格式。

可用属性列表：{valid_elements}

任务：
1. 从用户输入中识别出要查询的属性
2. 将属性名称标准化为完整形式（如"火"→"火系"，"水"→"水系"等）
3. 识别单属性或双属性查询
4. 输出JSON格式结果，包含与文档标签匹配的格式

用户输入：{user_input}

输出格式（JSON）：
{{
    "intent": "single_attribute" | "double_attribute" | "other",
    "attributes": ["属性1", "属性2"],
    "search_patterns": ["搜索模式1", "搜索模式2"],
    "standardized_query": "标准化查询描述"
}}

重要说明：
- 单属性搜索模式格式：["【火系】", "【水系】"] 等
- 双属性搜索模式格式：["【火系+水系】", "【水系+火系】"] 等
- 确保搜索模式能匹配知识库中实际的文档标签格式
"""